
spring:
  application:
    name: eureka-server

  cloud:
    config:
      profile: native
      uri: http://localhost:8888

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false


